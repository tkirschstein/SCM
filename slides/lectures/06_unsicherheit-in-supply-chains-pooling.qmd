---
title: "Unsicherheit in Supply Chains: Pooling"
---

```{r}
#| echo: false
#| warning: false
#| message: false

source("../../R/scm_functions.R")

library(tidyverse)
library(plotly)
library(knitr)
library(kableExtra)
```

---

## Die Natur der Unsicherheit {#slide-pooling-1}
::: slide-subtitle
Implizierte Unsicherheit in Supply Chains (Chopra & Meindl)
:::

::: {.callout-note appearance="minimal" icon=false}
**Implizierte Unsicherheit** (*implied uncertainty*) bezeichnet die Unsicherheit, mit der eine Supply Chain umgehen **muss** — bestimmt durch die Kombination aus **Nachfrageunsicherheit** und den **Serviceanforderungen** des Marktes (Chopra & Meindl, Kap. 2).
:::

::: columns
::: {.column width="52%"}

::: {.fragment .fade-up}
::: smaller-text
**Zwei Quellen implizierter Unsicherheit**

| Dimension | Niedrig | Hoch |
|---|---|---|
| **Nachfrageunsicherheit** | Salz, Strom | Fashion, Elektronik |
| **Liefervariabilität** | Beton, Kohle | Halbleiter, Fischfang |

$\rightarrow$ Hohe implizierte Unsicherheit erfordert **reaktionsfähige** (*responsive*) Supply Chains mit Pufferkapazität.
:::
:::

:::
::: {.column width="46%"}

::: {.fragment .fade-up}

```{r implied-uncertainty-plot}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 3.5

# Implied Uncertainty Spectrum nach Chopra & Meindl
produkte <- data.frame(
  label     = c("Salz", "Suppe", "Bier", "Kleidung",
                "PC", "Fashion", "Telefonchips"),
  iu_score  = c(0.05, 0.18, 0.30, 0.52, 0.65, 0.82, 0.94),
  kategorie = c("Functional", "Functional", "Functional",
                "Hybrid", "Hybrid", "Innovative", "Innovative")
)

farben <- c("Functional" = "#52B788",
            "Hybrid"     = "#E9C46A",
            "Innovative" = "#E76F51")

fig_iu <- plot_ly(produkte,
  x    = ~iu_score,
  y    = rep(1, nrow(produkte)),
  type = "scatter", mode = "markers+text",
  color = ~kategorie, colors = farben,
  marker = list(size = 22, opacity = 0.9,
                line = list(color = "white", width = 1.5)),
  text        = ~label,
  textposition = "top center",
  textfont    = list(size = 12, family = "Arial", color = "#1F2933")
) %>%
  layout(
    xaxis = list(
      title    = "Implizierte Unsicherheit →",
      range    = c(-0.05, 1.05),
      tickvals = c(0, 0.5, 1),
      ticktext = c("Niedrig", "Mittel", "Hoch"),
      fixedrange = TRUE
    ),
    yaxis = list(
      visible    = FALSE,
      range      = c(0.8, 1.5),
      fixedrange = TRUE
    ),
    legend = list(orientation = "h", y = -0.15, title = list(text = "Produkttyp")),
    annotations = list(
      list(
        x = 0.12, y = 0.82, xref = "x", yref = "paper",
        text = "<b>Efficient SC</b><br>wenig Puffer nötig",
        showarrow = FALSE,
        font = list(size = 11, color = "#52B788", family = "Arial"),
        align = "center"
      ),
      list(
        x = 0.88, y = 0.82, xref = "x", yref = "paper",
        text = "<b>Responsive SC</b><br>Puffer & Flexibilität",
        showarrow = FALSE,
        font = list(size = 11, color = "#E76F51", family = "Arial"),
        align = "center"
      )
    ),
    shapes = list(list(
      type = "line",
      x0 = 0.5, x1 = 0.5, y0 = 0, y1 = 1,
      xref = "x", yref = "paper",
      line = list(color = "#9CA3AF", dash = "dot", width = 1.2)
    )),
    plot_bgcolor  = "transparent",
    paper_bgcolor = "transparent",
    margin = list(l = 30, r = 20, t = 20, b = 60),
    height = 330
  )

fig_iu
```

:::

:::
:::

::: {.fragment .fade-up}
::: {.callout-warning appearance="simple"}
## Das Grunddilemma
- **Nachfrage** ist variabel und schwer vorhersagbar
- **Kapazitäten** (Werke, Lager, Personal) werden im Voraus festgelegt
- $\rightarrow$ Mismatch zwischen Angebot und Nachfrage ist **strukturell unvermeidlich**
:::
:::

---

## Die Natur der Unsicherheit {#slide-pooling-mismatch}
::: slide-subtitle
Mismatch: Schwankende Nachfrage vs. fixe Kapazität
:::

::: columns
::: {.column width="52%"}

::: {.callout-note appearance="minimal" icon=false}
Kapazitäten (Maschinen, Personal, Lagerraum) werden **ex ante** bestimmt. Die Nachfrage realisiert sich **ex post**. Das Ergebnis: **systematischer Mismatch** in beide Richtungen.
:::

::: {.fragment .fade-up}
::: smaller-text

**Konsequenzen des Mismatches**

| Szenario | Phänomen | Folge |
|---|---------|---------| 
| $d > C$ | Überlastung | Lost Sales, Umsatzverlust |
| $d < C$ | Unterauslastung | Leerkosten |
| $d \approx C$ | Gleichgewicht |  bei perfekter Prognose |

:::
:::

:::
::: {.column width="46%"}

::: {.fragment .fade-up}

```{r mismatch-plot}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 3.5

set.seed(12)
n_periods <- 24
mu_d <- 88
sigma_d <- 14
C <- 90

t_vals <- 1:n_periods
d_vals <- pmax(40, round(rnorm(n_periods, mu_d, sigma_d)))
prod_vals <- pmin(d_vals, C)
lost_vals <- pmax(0, d_vals - C)
idle_vals <- pmax(0, C - d_vals)

df_mm <- data.frame(
  t = t_vals, d = d_vals, prod = prod_vals,
  lost = lost_vals, idle = idle_vals
)

fig_mm <- plot_ly(df_mm, x = ~t)

# Kapazitätsgrenze
fig_mm <- fig_mm %>% add_trace(
  x = c(1, n_periods), y = c(C, C),
  type = "scatter", mode = "lines",
  name = paste0("Kapazität C = ", C),
  line = list(color = "#6B7280", width = 1.8, dash = "dash"),
  hoverinfo = "skip"
)

# Leerkapazität (shaded)
fig_mm <- fig_mm %>% add_trace(
  x = ~t, y = ~idle,
  type = "bar", name = "Idle Capacity (C − d)",
  marker = list(color = "rgba(234,179,8,0.45)",
                line = list(color = "rgba(234,179,8,0.0)", width = 0)),
  base = ~prod
)

# Lost Sales (shaded above C)
fig_mm <- fig_mm %>% add_trace(
  x = ~t, y = ~lost,
  type = "bar", name = "Lost Sales (d − C)",
  marker = list(color = "rgba(220,38,38,0.55)",
                line = list(color = "rgba(220,38,38,0.0)", width = 0)),
  base = C
)

# Produktion
fig_mm <- fig_mm %>% add_trace(
  x = ~t, y = ~prod,
  type = "scatter", mode = "lines",
  name = "Produktion / Verkauf",
  line = list(color = cols$top, width = 2)
)

# Nachfrage
fig_mm <- fig_mm %>% add_trace(
  x = ~t, y = ~d,
  type = "scatter", mode = "lines+markers",
  name = "Nachfrage d",
  line   = list(color = "#1D4ED8", width = 1.5),
  marker = list(color = "#1D4ED8", size = 5)
)

fig_mm <- fig_mm %>% layout(
  barmode = "overlay",
  xaxis = list(title = "Periode", dtick = 3, fixedrange = TRUE),
  yaxis = list(title = "Einheiten",
               range = c(40, max(d_vals) * 1.12),
               fixedrange = TRUE),
  legend = list(orientation = "h", y = -0.22,
                font = list(size = 11)),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 50, r = 20, t = 10, b = 70),
  height = 370,
  width = 650
)

fig_mm
```

:::

:::
:::

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## Praxisbeispiele
- **Automobilindustrie:** Werke produzieren nur bestimmte Modelle für mehrere Jahre $\rightarrow$ Auslastung hängt von Nachfrage ab
- **Einzelhandel:** Saisonware (Weihnachtsartikel) wird zu früh bestellt, Reste werden verramscht
- **Krankenhaus:** OP-Kapazitäten sind täglich fix, Notfallaufkommen ist hochvariabel
:::
:::



---

## Umgang mit Unsicherheit {#slide-cp-background}
::: slide-subtitle
Kapazitätspooling
:::

::: {.callout-note appearance="simple"}
## Ausgangssituation (Cachon & Terwiesch, Kap. 17)
Ein Automobilhersteller (z.B. GM) betreibt **2 Produktionswerke** für **2 Fahrzeugmodelle** (z.B. Chevy Malibu, GMC Yukon XL). Jedes Werk ist genau einem Modell zugeordnet – **keine Flexibilität**. Die Kapazität je Werk beträgt **90 Einheiten**. Die Nachfrage ist **unsicher** und wird beobachtet, nachdem die Kapazitäten festgelegt wurden.
:::

::: columns
::: {.column width="50%"}

::: {.fragment .fade-up}
::: {.callout-warning appearance="simple"}
## Konsequenzen der Inflexibilität
- Übersteigt die Nachfrage die Kapazität $\rightarrow$ **Fehlmenge** (Lost Sales)
- Liegt die Nachfrage unter der Kapazität $\rightarrow$ **Leerkapazität** (Idle Capacity)
- Beide Effekte reduzieren die **Systemperformance**
:::
:::

:::
::: {.column width="48%"}

::: {.fragment .fade-up}

```{r cp-mismatch-setup}
#| echo: false
#| warning: false
#| message: false

# Empirische Nachfragedaten aus cp_data (Monate 1–12)
cp_data_bg <- data.frame(
  Monat = 1:12,
  dA    = c(78, 79, 92, 76, 95, 104, 87, 92, 91, 85, 96, 93),
  dB    = c(102, 92, 93, 87, 80,  97, 90, 76, 89, 86, 89, 93)
)
C_bg <- 90

make_mismatch_fig <- function(d_vals, C, werk_label, farbe_linie,
                               height = 290, width = 600) {
  t_vals    <- seq_along(d_vals)
  prod_vals <- pmin(d_vals, C)
  lost_vals <- pmax(0, d_vals - C)
  idle_vals <- pmax(0, C - d_vals)

  df <- data.frame(t = t_vals, d = d_vals, prod = prod_vals,
                   lost = lost_vals, idle = idle_vals)

  fig <- plot_ly(df, x = ~t)

  # Kapazitätsgrenze
  fig <- fig %>% add_trace(
    x = c(1, max(t_vals)), y = c(C, C),
    type = "scatter", mode = "lines",
    name = paste0("C = ", C),
    line = list(color = "#6B7280", width = 1.6, dash = "dash"),
    hoverinfo = "skip",
    showlegend = TRUE
  )

  # Leerkapazität (gelb)
  fig <- fig %>% add_trace(
    x = ~t, y = ~idle,
    type = "bar", name = "Idle Capacity (C − d)",
    marker = list(color = "rgba(234,179,8,0.45)",
                  line = list(color = "rgba(234,179,8,0.0)", width = 0)),
    base = ~prod
  )

  # Lost Sales (rot)
  fig <- fig %>% add_trace(
    x = ~t, y = ~lost,
    type = "bar", name = "Lost Sales (d − C)",
    marker = list(color = "rgba(220,38,38,0.55)",
                  line = list(color = "rgba(220,38,38,0.0)", width = 0)),
    base = C
  )

  # Produktion / Verkauf
  fig <- fig %>% add_trace(
    x = ~t, y = ~prod,
    type = "scatter", mode = "lines",
    name = "Produktion",
    line = list(color = farbe_linie, width = 2)
  )

  # Nachfragelinie
  fig <- fig %>% add_trace(
    x = ~t, y = ~d,
    type = "scatter", mode = "lines+markers",
    name = "Nachfrage d",
    line   = list(color = "#1D4ED8", width = 1.4),
    marker = list(color = "#1D4ED8", size = 5)
  )

  fig <- fig %>% layout(
    barmode = "overlay",
    title   = list(text = paste0("<b>", werk_label, "</b>"),
                   font = list(size = 13, color = "#1F2933", family = "Arial"),
                   x = 0.02, xanchor = "left"),
    xaxis   = list(title = "Monat", dtick = 2, fixedrange = TRUE,
                   range = c(0.5, max(t_vals) + 0.5)),
    yaxis   = list(title = "Einheiten",
                   range = c(60, max(d_vals) * 1.15),
                   fixedrange = TRUE),
    legend  = list(orientation = "h", y = -0.30,
                   font = list(size = 10)),
    plot_bgcolor  = "transparent",
    paper_bgcolor = "transparent",
    margin = list(l = 45, r = 10, t = 30, b = 65),
    height = height
  )

  fig
}
```

```{r cp-mismatch-werkA}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 2.8

make_mismatch_fig(
  d_vals      = cp_data_bg$dA,
  C           = C_bg,
  werk_label  = "Werk 1 – Chevy Malibu",
  farbe_linie = cols$top,
  height      = 270,
  width       = 600
)
```

```{r cp-mismatch-werkB}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 2.8

make_mismatch_fig(
  d_vals      = cp_data_bg$dB,
  C           = C_bg,
  werk_label  = "Werk 2 – GMC Yukon XL",
  farbe_linie = cols$mid,
  height      = 270,
  width       = 600
)
```

:::

:::
:::

---

## Kapazitätspooling {#slide-cp-original}
::: slide-subtitle
Originalkonfiguration – Jedes Werk produziert nur ein Modell
:::

**Monatliche Nachfrage und Produktion (C¹ = C² = 90)**

::: columns
::: {.column width="25%"}

::: small-text

```{r cp-original-table}
#| echo: false
#| warning: false
#| message: false

C1 <- C2 <- 90

cp_data <- data.frame(
  Monat = 1:12,
  dA    = c(78, 79, 92, 76, 95,104, 87, 92, 91, 85, 96, 93),
  dB    = c(102, 92, 93, 87, 80, 97, 90, 76, 89, 86, 89, 93),
  xA    = c(78, 79, 90, 76, 90, 90, 87, 90, 90, 85, 90, 90),
  xB    = c(90, 90, 90, 87, 80, 90, 90, 76, 89, 86, 89, 90)
)

cp.means <- colMeans(cp_data)
cp.means[1] <- NA

cp_data <- rbind(cp_data,cp.means)

cp_data$Monat <- as.character(cp_data$Monat)

cp_data$Monat[13] <- "&#8960;"

kbl(cp_data,
    col.names = c("Monat", "d<sub>t</sub><sup>A</sup>", "d<sub>t</sub><sup>B</sup>",
                  "x<sub>t</sub><sup>A</sup>", "x<sub>t</sub><sup>B</sup>"),
    align = c("c","r","r","r","r"),
    escape = FALSE) |>
  kable_styling(bootstrap_options = c("condensed","striped"),
                full_width = TRUE, font_size = 20) |>
  column_spec(1, bold = TRUE, background = "#2F3E46", color = "white") |>
  row_spec(0, background = "#2F3E46", color = "white", bold = TRUE) |>
  row_spec(13, bold = TRUE, background = "#2F3E4640")
```

:::
:::


::: {.column width="27%"}

```{r cp-original-graph}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 3

# Original-Setting: C1->1->A, C2->2->B (getrennte Linien, kein Pooling)
plants <- data.frame(
  id  = c("P1","P2"),
  lbl = c("1","2"),
  cl  = c("C<sup>1</sup>","C<sup>2</sup>"),
  x   = 0.35,
  y   = c(0.72, 0.28)
)

markets <- data.frame(
  id  = c("A","B"),
  lbl = c("A","B"),
  dj  = c("d<sup>A</sup>","d<sup>B</sup>"),
  x   = 0.72,
  y   = c(0.72, 0.28)
)

pxy <- setNames(plants[,  c("x","y")], c("x","y")); rownames(pxy) <- plants$id
mxy <- setNames(markets[, c("x","y")], c("x","y")); rownames(mxy) <- markets$id

r <- 0.055  # Knotenradius in Plot-Koordinaten

edges <- data.frame(
  from = c("P1","P2"),
  to   = c("A","B"),
  lbl  = c("x<sup>1A</sup>","x<sup>2B</sup>"),
  dash = c(FALSE, FALSE),
  stringsAsFactors = FALSE
)

fig <- plot_ly(type = "scatter", mode = "markers")

for (i in seq_len(nrow(edges))) {
  x0 <- pxy[edges$from[i], "x"];  y0 <- pxy[edges$from[i], "y"]
  x1 <- mxy[edges$to[i],   "x"];  y1 <- mxy[edges$to[i],   "y"]
  dx <- x1 - x0;  dy <- y1 - y0;  d <- sqrt(dx^2 + dy^2)
  ax <- x0 + r * dx / d;  ay <- y0 + r * dy / d
  ex <- x1 - r * dx / d;  ey <- y1 - r * dy / d

  fig <- fig %>% add_annotations(
    x = ex, y = ey, ax = ax, ay = ay,
    xref = "x", yref = "y", axref = "x", ayref = "y",
    showarrow = TRUE, arrowhead = 2, arrowsize = 1,
    arrowwidth = 1.5, arrowcolor = "#4B4B4B", text = ""
  )

  if (nchar(edges$lbl[i]) > 0) {
    fig <- fig %>% add_annotations(
      x = (ax + ex) / 2, y = (ay + ey) / 2 + 0.06,
      xref = "x", yref = "y",
      text = edges$lbl[i], showarrow = FALSE,
      font = list(size = 13, color = "#1F2933", family = "Arial")
    )
  }
}

# Kostenlabels links der Pflanzen
fig <- fig %>% add_annotations(
  x = plants$x - 0.12, y = plants$y,
  xref = "x", yref = "y",
  text = plants$cl, showarrow = FALSE,
  font = list(size = 14, color = "#1F2933", family = "Arial"),
  xanchor = "right"
)

# Pflanzen-Knoten (Hexagon)
fig <- fig %>% add_trace(
  x = plants$x, y = plants$y, text = plants$lbl,
  mode = "markers+text",
  marker = list(symbol = "hexagon", size = 42,
                color = "#BFDBF7", line = list(color = "#4B5563", width = 1.5)),
  textfont = list(size = 16, color = "#1F2933", family = "Arial"),
  textposition = "middle center",
  hoverinfo = "skip", showlegend = FALSE
)

# Markt-Knoten (Circle)
fig <- fig %>% add_trace(
  x = markets$x, y = markets$y, text = markets$lbl,
  mode = "markers+text",
  marker = list(symbol = "circle", size = 42,
                color = "#2D9E8E", line = list(color = "#2F3E46", width = 1.5)),
  textfont = list(size = 16, color = "white", family = "Arial"),
  textposition = "middle center",
  hoverinfo = "skip", showlegend = FALSE
)

# Nachfragelabels rechts der Märkte
fig <- fig %>% add_annotations(
  x = markets$x + 0.09, y = markets$y,
  xref = "x", yref = "y",
  text = markets$dj, showarrow = FALSE,
  font = list(size = 14, color = "#1F2933", family = "Arial"),
  xanchor = "left"
)

# Set-Labels oben
fig <- fig %>% add_annotations(
  x = c(0.35, 0.72), y = c(1.00, 1.00),
  xref = "x", yref = "y",
  text = c("<b>Plant / vehicle type</b>", "<b>vehicle market</b>"),
  showarrow = FALSE,
  font = list(size = 13, color = "#1F2933", family = "Arial")
)

fig <- fig %>% layout(
  xaxis = list(visible = FALSE, range = c(0, 1), fixedrange = TRUE),
  yaxis = list(visible = FALSE, range = c(0, 1.08),  fixedrange = TRUE),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 50, r = 50, t = 10, b = 10),
  height = 250,
  width  = 400
)

fig
```
:::


::: {.column width="46%"}

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## System Evaluation

- **Erwartete Nachfrage:** $E(d^A)=$ `r round(mean(cp_data$dA),1)`; $E(d^B)=$ `r round(mean(cp_data$dB),1)`
- **Erwartete Produktion/Verkauf:** $E(x^A)=$ `r round(mean(cp_data$xA),1)`; $E(x^B)=$ `r round(mean(cp_data$xB),1)`
- **Werksauslastung:** $\frac{E(x^A)}{C^1}=$ `r round(mean(cp_data$xA)/C1*100,1)` %; $\frac{E(x^B)}{C^2}=$ `r round(mean(cp_data$xB)/C2*100,1)` %
- **Erwarteter Lieferservicegrad:** $\frac{E(x^A)}{E(d^A)}=$ `r round(mean(cp_data$xA/cp_data$dA)*100,1)` %; $\frac{E(x^B)}{E(d^B)}=$ `r round(mean(cp_data$xB/cp_data$dB)*100,1)` %
:::
:::


:::
:::

::: {.fragment .fade-up}
::: {.callout-warning appearance="simple"}
## Problem
Auslastung und Lieferservicegrad sind hoch, dennoch gibt es **lost sales** und **Unterauslastung** $\rightarrow$ Kann gegenseitiges Aushelfen (**Kapazitätspooling**) die Kennzahlen verbessern?
:::
:::


---

## Kapazitätspooling {#slide-cp-pooling}
::: slide-subtitle
Einseitiges Pooling – Werk 1 kann beide Modelle produzieren
:::

::: columns
::: {.column width="25%"}

::: small-text
```{r cp-pooling-table}
#| echo: false
#| warning: false
#| message: false

C1 <- 90; C2 <- 90

cp_pool_1 <- cp_data[-13,] %>%
  mutate(
    x1A = pmin(dA, C1),
    x1B = ifelse(dB > C2 & dA < C1,
                 pmin(C1 - dA, dB - C2), 0),
    x1  = x1A + x1B,
    x2B = pmin(C2, dB),
    xB =  x2B + x1B
  )

kbl(cp_pool_1[, c("Monat","dA","dB","x1A","x1B","x2B")],
    col.names = c("Monat", "d<sub>t</sub><sup>A</sup>", "d<sub>t</sub><sup>B</sup>",
                  "x<sub>t</sub><sup>1A</sup>", "x<sub>t</sub><sup>1B</sup>", "x<sub>t</sub><sup>2B</sup>"),
    align = c("c","r","r","r","r","r"),
    escape = FALSE) |>
  kable_styling(bootstrap_options = c("condensed","striped"),
                full_width = TRUE, font_size = 22) |>
  column_spec(1, bold = TRUE, background = "#2F3E46", color = "white") |>
  column_spec(4:5, background = "#E8F4F1") |>
  row_spec(0, background = "#2F3E46", color = "white", bold = TRUE)
```
:::
:::


::: {.column width="27%"}

```{r cp-pooling-graph}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 3

# Pooling-Setting: C1->1->A (solid), C1->1->B (gestrichelt), C2->2->B (solid)
plants <- data.frame(
  id  = c("P1","P2"),
  lbl = c("1","2"),
  cl  = c("C<sup>1</sup>","C<sup>2</sup>"),
  x   = 0.35,
  y   = c(0.72, 0.28)
)

markets <- data.frame(
  id  = c("A","B"),
  lbl = c("A","B"),
  dj  = c("d<sup>A</sup>","d<sup>B</sup>"),
  x   = 0.72,
  y   = c(0.72, 0.28)
)

pxy <- setNames(plants[,  c("x","y")], c("x","y")); rownames(pxy) <- plants$id
mxy <- setNames(markets[, c("x","y")], c("x","y")); rownames(mxy) <- markets$id

r <- 0.06  # Knotenradius in Plot-Koordinaten

edges <- data.frame(
  from  = c("P1","P1","P2"),
  to    = c("A",  "B",  "B"),
  lbl   = c("x<sup>1A</sup>", "x<sup>1B</sup>", "x<sup>2B</sup>"),
  dash  = c(FALSE, TRUE, FALSE),   # TRUE = gestrichelt (Pooling-Pfeil)
  stringsAsFactors = FALSE
)

fig <- plot_ly(type = "scatter", mode = "markers")

for (i in seq_len(nrow(edges))) {
  x0 <- pxy[edges$from[i], "x"];  y0 <- pxy[edges$from[i], "y"]
  x1 <- mxy[edges$to[i],   "x"];  y1 <- mxy[edges$to[i],   "y"]
  dx <- x1 - x0;  dy <- y1 - y0;  d <- sqrt(dx^2 + dy^2)
  ax <- x0 + r * dx / d;  ay <- y0 + r * dy / d
  ex <- x1 - r * dx / d;  ey <- y1 - r * dy / d

  # Gestrichelter Pfeil via Liniensegmente (Plotly unterstützt kein dash bei add_annotations)
  if (edges$dash[i]) {
    # Segmentierte gestrichelte Linie mit 6 Abschnitten
    n_seg <- 10
    xs <- seq(ax, ex, length.out = n_seg + 1)
    ys <- seq(ay, ey, length.out = n_seg + 1)
    for (s in seq(1, n_seg, by = 2)) {
      fig <- fig %>% add_segments(
        x = xs[s], xend = xs[s+1],
        y = ys[s], yend = ys[s+1],
        line = list(color = "#4B4B4B", width = 1.8, dash = "solid"),
        hoverinfo = "skip", showlegend = FALSE
      )
    }
    # Pfeilspitze am Endpunkt
    fig <- fig %>% add_annotations(
      x = ex, y = ey, ax = xs[n_seg], ay = ys[n_seg],
      xref = "x", yref = "y", axref = "x", ayref = "y",
      showarrow = TRUE, arrowhead = 2, arrowsize = 1,
      arrowwidth = 1.8, arrowcolor = "#4B4B4B", text = ""
    )
  } else {
    fig <- fig %>% add_annotations(
      x = ex, y = ey, ax = ax, ay = ay,
      xref = "x", yref = "y", axref = "x", ayref = "y",
      showarrow = TRUE, arrowhead = 2, arrowsize = 1,
      arrowwidth = 1.5, arrowcolor = "#4B4B4B", text = ""
    )
  }

  # Kantenlabel
  if (nchar(edges$lbl[i]) > 0) {
    # x^1B-Label leicht versetzt (Überkreuzungspfeil)
    lbl_off_x <- ifelse(edges$dash[i],  0.03, 0.00)
    lbl_off_y <- ifelse(edges$dash[i], -0.05, 0.06)
    fig <- fig %>% add_annotations(
      x = (ax + ex) / 2 + lbl_off_x,
      y = (ay + ey) / 2 + lbl_off_y,
      xref = "x", yref = "y",
      text = edges$lbl[i], showarrow = FALSE,
      font = list(size = 13, color = "#1F2933", family = "Arial")
    )
  }
}

# Kostenlabels links der Pflanzen
fig <- fig %>% add_annotations(
  x = plants$x - 0.12, y = plants$y,
  xref = "x", yref = "y",
  text = plants$cl, showarrow = FALSE,
  font = list(size = 14, color = "#1F2933", family = "Arial"),
  xanchor = "right"
)

# Pflanzen-Knoten (Hexagon)
fig <- fig %>% add_trace(
  x = plants$x, y = plants$y, text = plants$lbl,
  mode = "markers+text",
  marker = list(symbol = "hexagon", size = 42,
                color = "#BFDBF7", line = list(color = "#4B5563", width = 1.5)),
  textfont = list(size = 16, color = "#1F2933", family = "Arial"),
  textposition = "middle center",
  hoverinfo = "skip", showlegend = FALSE
)

# Markt-Knoten (Circle)
fig <- fig %>% add_trace(
  x = markets$x, y = markets$y, text = markets$lbl,
  mode = "markers+text",
  marker = list(symbol = "circle", size = 42,
                color = "#2D9E8E", line = list(color = "#2F3E46", width = 1.5)),
  textfont = list(size = 16, color = "white", family = "Arial"),
  textposition = "middle center",
  hoverinfo = "skip", showlegend = FALSE
)

# Nachfragelabels rechts der Märkte
fig <- fig %>% add_annotations(
  x = markets$x + 0.09, y = markets$y,
  xref = "x", yref = "y",
  text = markets$dj, showarrow = FALSE,
  font = list(size = 14, color = "#1F2933", family = "Arial"),
  xanchor = "left"
)

# Set-Labels oben
fig <- fig %>% add_annotations(
  x = c(0.35, 0.72), y = c(1.00, 1.00),
  xref = "x", yref = "y",
  text = c("<b>Plant / vehicle type</b>", "<b>vehicle market</b>"),
  showarrow = FALSE,
  font = list(size = 13, color = "#1F2933", family = "Arial")
)

fig <- fig %>% layout(
  xaxis = list(visible = FALSE, range = c(0.2, .9), fixedrange = TRUE),
  yaxis = list(visible = FALSE, range = c(0.2, 1),  fixedrange = TRUE),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 50, r = 50, t = 10, b = 10),
  height = 250,
  width  = 400
)

fig
```
:::


::: {.column width="46%"}

::: {.fragment .fade-up}
::: {.callout-note appearance="minimal" icon=false}
**Pooling-Regel** (wenn $d_t^B > C^2$ und $d_t^A < C^1$):

$$x_t^1 = d_t^A + \min(C^1 - d_t^A;\; d_t^B - C^2)$$
$$x_t^{2B} = C^2$$
:::
:::

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## System Evaluation (Pooling)

- **Produktion Werk 1:** $E(x^1)=$ `r round(mean(cp_pool_1$x1),1)`
- **Versorgung Markt B:** $E(x^{1B}+x^{2B})=$ `r round(mean(cp_pool_1$xB),1)`
- **Auslastung Werk 1:** $\frac{E(x^1)}{C^1}=$ `r round(mean(cp_pool_1$x1)/C1*100,1)` %
- **Lieferservicegrad Markt B:** $\frac{E(x^{1B}+x^{2B})}{E(d^B)}=$ `r round(mean(cp_pool_1$xB)/mean(cp_pool_1$dB)*100,1)` %
:::
:::

:::
:::

---


## Kapazitätspooling {#slide-cp-pooling}
::: slide-subtitle
Zweiseitiges Pooling – Beide Werke können beide Modelle produzieren
:::


:::: columns
::: {.column width="27%"}

::: small-text
```{r cp-pooling-table2}
#| echo: false
#| warning: false
#| message: false

cp_pool <- cp_data[-13,] %>%
  mutate(
    x1A = pmin(dA, C1),
    x1B = ifelse(dB > C2 & dA < C1,
                 pmin(C1 - dA, dB - C2), 0),
    x1  = x1A + x1B,
    x2B = pmin(C2, dB),
    x2A = ifelse(dA > C1 & dB < C2,
                 pmin(C2 - dB, dA - C1), 0),
    x2  = x2A + x2B,
    xA = x2A + x1A,
    xB = x2B + x1B
  )

kbl(cp_pool[, c("Monat","dA","dB","x1A","x1B","x2B", "x2A")],
    col.names = c("Monat", "d<sub>t</sub><sup>A</sup>", "d<sub>t</sub><sup>B</sup>",
                  "x<sub>t</sub><sup>1A</sup>", "x<sub>t</sub><sup>1B</sup>", "x<sub>t</sub><sup>2B</sup>", "x<sub>t</sub><sup>2A</sup>"),
    align = c("c","r","r","r","r","r","r"),
    escape = FALSE) |>
  kable_styling(bootstrap_options = c("condensed","striped"),
                full_width = TRUE, font_size = 22) |>
  column_spec(1, bold = TRUE, background = "#2F3E46", color = "white") |>
  column_spec(4:5, background = "#E8F4F1") |>
  column_spec(6:7, background = "#E8F4A1") |>
  row_spec(0, background = "#2F3E46", color = "white", bold = TRUE)
```
:::
:::


::: {.column width="27%"}

```{r cp-pooling-graph2}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 3

# Pooling-Setting: C1->1->A (solid), C1->1->B (gestrichelt), C2->2->B (solid)
plants <- data.frame(
  id  = c("P1","P2"),
  lbl = c("1","2"),
  cl  = c("C<sup>1</sup>","C<sup>2</sup>"),
  x   = 0.35,
  y   = c(0.72, 0.28)
)

markets <- data.frame(
  id  = c("A","B"),
  lbl = c("A","B"),
  dj  = c("d<sup>A</sup>","d<sup>B</sup>"),
  x   = 0.72,
  y   = c(0.72, 0.28)
)

pxy <- setNames(plants[,  c("x","y")], c("x","y")); rownames(pxy) <- plants$id
mxy <- setNames(markets[, c("x","y")], c("x","y")); rownames(mxy) <- markets$id

r <- 0.06  # Knotenradius in Plot-Koordinaten

edges <- data.frame(
  from  = c("P1","P1","P2", "P2"),
  to    = c("A",  "B",  "B", "A"),
  lbl   = c("x<sup>1A</sup>", "x<sup>1B</sup>", "x<sup>2B</sup>",  "x<sup>2A</sup>"),
  dash  = c(FALSE, TRUE, FALSE, TRUE),   # TRUE = gestrichelt (Pooling-Pfeil)
  stringsAsFactors = FALSE
)

fig <- plot_ly(type = "scatter", mode = "markers")

for (i in seq_len(nrow(edges))) {
  x0 <- pxy[edges$from[i], "x"];  y0 <- pxy[edges$from[i], "y"]
  x1 <- mxy[edges$to[i],   "x"];  y1 <- mxy[edges$to[i],   "y"]
  dx <- x1 - x0;  dy <- y1 - y0;  d <- sqrt(dx^2 + dy^2)
  ax <- x0 + r * dx / d;  ay <- y0 + r * dy / d
  ex <- x1 - r * dx / d;  ey <- y1 - r * dy / d

  # Gestrichelter Pfeil via Liniensegmente (Plotly unterstützt kein dash bei add_annotations)
  if (edges$dash[i]) {
    # Segmentierte gestrichelte Linie mit 6 Abschnitten
    n_seg <- 10
    xs <- seq(ax, ex, length.out = n_seg + 1)
    ys <- seq(ay, ey, length.out = n_seg + 1)
    for (s in seq(1, n_seg, by = 2)) {
      fig <- fig %>% add_segments(
        x = xs[s], xend = xs[s+1],
        y = ys[s], yend = ys[s+1],
        line = list(color = "#4B4B4B", width = 1.8, dash = "solid"),
        hoverinfo = "skip", showlegend = FALSE
      )
    }
    # Pfeilspitze am Endpunkt
    fig <- fig %>% add_annotations(
      x = ex, y = ey, ax = xs[n_seg], ay = ys[n_seg],
      xref = "x", yref = "y", axref = "x", ayref = "y",
      showarrow = TRUE, arrowhead = 2, arrowsize = 1,
      arrowwidth = 1.8, arrowcolor = "#4B4B4B", text = ""
    )
  } else {
    fig <- fig %>% add_annotations(
      x = ex, y = ey, ax = ax, ay = ay,
      xref = "x", yref = "y", axref = "x", ayref = "y",
      showarrow = TRUE, arrowhead = 2, arrowsize = 1,
      arrowwidth = 1.5, arrowcolor = "#4B4B4B", text = ""
    )
  }

  # Kantenlabel
  # x^1B-Label leicht versetzt (Überkreuzungspfeil)
  lbl_off_x <- ifelse(i >= 3, ifelse(i==3, 0.03 , -0.02), 0.00)
  lbl_off_y <- ifelse(i >= 3, ifelse(i==3, -0.05 , -0.08), 0.06)
  fig <- fig %>% add_annotations(
    x = (ax + ex) / 2 + lbl_off_x,
    y = (ay + ey) / 2 + lbl_off_y,
    xref = "x", yref = "y",
    text = edges$lbl[i], showarrow = FALSE,
    font = list(size = 13, color = "#1F2933", family = "Arial")
  )
  
}

# Kostenlabels links der Pflanzen
fig <- fig %>% add_annotations(
  x = plants$x - 0.12, y = plants$y,
  xref = "x", yref = "y",
  text = plants$cl, showarrow = FALSE,
  font = list(size = 14, color = "#1F2933", family = "Arial"),
  xanchor = "right"
)

# Pflanzen-Knoten (Hexagon)
fig <- fig %>% add_trace(
  x = plants$x, y = plants$y, text = plants$lbl,
  mode = "markers+text",
  marker = list(symbol = "hexagon", size = 42,
                color = "#BFDBF7", line = list(color = "#4B5563", width = 1.5)),
  textfont = list(size = 16, color = "#1F2933", family = "Arial"),
  textposition = "middle center",
  hoverinfo = "skip", showlegend = FALSE
)

# Markt-Knoten (Circle)
fig <- fig %>% add_trace(
  x = markets$x, y = markets$y, text = markets$lbl,
  mode = "markers+text",
  marker = list(symbol = "circle", size = 42,
                color = "#2D9E8E", line = list(color = "#2F3E46", width = 1.5)),
  textfont = list(size = 16, color = "white", family = "Arial"),
  textposition = "middle center",
  hoverinfo = "skip", showlegend = FALSE
)

# Nachfragelabels rechts der Märkte
fig <- fig %>% add_annotations(
  x = markets$x + 0.09, y = markets$y,
  xref = "x", yref = "y",
  text = markets$dj, showarrow = FALSE,
  font = list(size = 14, color = "#1F2933", family = "Arial"),
  xanchor = "left"
)

# Set-Labels oben
fig <- fig %>% add_annotations(
  x = c(0.35, 0.72), y = c(1.00, 1.00),
  xref = "x", yref = "y",
  text = c("<b>Plant / vehicle type</b>", "<b>vehicle market</b>"),
  showarrow = FALSE,
  font = list(size = 13, color = "#1F2933", family = "Arial")
)

fig <- fig %>% layout(
  xaxis = list(visible = FALSE, range = c(0.2, .9), fixedrange = TRUE),
  yaxis = list(visible = FALSE, range = c(0.2, 1),  fixedrange = TRUE),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 50, r = 50, t = 10, b = 10),
  height = 250,
  width  = 400
)

fig
```
:::


::: {.column width="46%"}

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## System Evaluation

- **Produktion Werk 1:** $E(x^1)=$ `r round(mean(cp_pool$x1),1)`
- **Produktion Werk 2:** $E(x^2)=$ `r round(mean(cp_pool$x2),1)`
- **Versorgung Markt A:** $E(x^{1A}+x^{2A})=$ `r round(mean(cp_pool$xA),1)`
- **Versorgung Markt B:** $E(x^{1B}+x^{2B})=$ `r round(mean(cp_pool$xB),1)`
- **Auslastung Werk 1:** $\frac{E(x^1)}{C^1}=$ `r round(mean(cp_pool$x1)/C1*100,1)` % 
- **Auslastung Werk 2:** $\frac{E(x^2)}{C^2}=$ `r round(mean(cp_pool$x2)/C2*100,1)` % 
- **Lieferservicegrad Markt A:** $\frac{E(x^{1A}+x^{2A})}{E(d^A)}=$ `r round(mean(cp_pool$xA)/mean(cp_pool$dA)*100,1)` % 
- **Lieferservicegrad Markt B:** $\frac{E(x^{2B}+x^{1B})}{E(d^B)}=$  `r round(mean(cp_pool$xB)/mean(cp_pool$dB)*100,1)` % 
:::
:::

:::
::::

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
Versorgungssystem reagiert als wäre die Trennung zwischen Märkten/Produkten und Werken aufgehoben $\rightarrow$ Gesamtkapazität und Gesamtnachfrage entscheidend
:::
:::

---

## Kapazitätspooling {#slide-cp-kpis}
::: slide-subtitle
Kennzahlenvergleich
:::

```{r cp-kpi-compute}
#| echo: false
#| warning: false
#| message: false


cp_data <- cp_data[-13,]

tot.demand <- c(sum(cp_data$dA), sum(cp_data$dB))

kpi_df <- data.frame(
  Kennzahl = c(
    "Ø Produktion Werk 1 (Einh.)",
    "Ø Produktion Werk 2 (Einh.)",
    "Ø Verkäufe Markt A (Einh.)",
    "Ø Verkäufe Markt B (Ei,nh.)",
    "Lost Sales Markt A (Einh.)",
    "Lost Sales Markt B (Einh.)",
    "Auslastung Werk 1 (%)",
    "Auslastung Werk 2 (%)",
    "Verkaufsquote Markt A (%)",
    "Verkaufsquote Markt B (%)"
  ),
  Dezentral = c(
    round(mean(cp_data$xA), 1),
    round(mean(cp_data$xB), 1),
    round(mean(cp_data$xA), 1),
    round(mean(cp_data$xB), 1),
    sum(cp_data$dA)-sum(cp_data$xA),
    sum(cp_data$dB)-sum(cp_data$xB),
    round(mean(cp_data$xA/C1*100), 1),
    round(mean(cp_data$xB/C2*100), 1),
    round(sum(cp_data$xA)/tot.demand[1]*100, 1),
    round(sum(cp_data$xB)/tot.demand[2]*100, 1)
  ),
  "Pooling einseitig" = c(
    round(mean(cp_pool_1$x1), 1),
    round(mean(cp_pool_1$x2B), 1),
    round(mean(cp_pool_1$xA), 1),
    round(mean(cp_pool_1$xB), 1),
    sum(cp_pool_1$dA)-sum(cp_pool_1$xA),
    sum(cp_pool_1$dB)-sum(cp_pool_1$xB),
    round(mean(cp_pool_1$xA/C1*100), 1),
    round(mean(cp_pool_1$xB/C2*100), 1),
    round(sum(cp_pool_1$xA)/tot.demand[1]*100, 1),
    round(sum(cp_pool_1$xB)/tot.demand[2]*100, 1)
  ),
  "Pooling zweiseitig" = c(
    round(mean(cp_pool$x1), 1),
    round(mean(cp_pool$x2), 1),
    round(mean(cp_pool$xA), 1),
    round(mean(cp_pool$xB), 1),
    sum(cp_pool$dA)-sum(cp_pool$xA),
    sum(cp_pool$dB)-sum(cp_pool$xB),
    round(mean(cp_pool$xA/C1*100), 1),
    round(mean(cp_pool$xB/C2*100), 1),
    round(sum(cp_pool$xA)/tot.demand[1]*100, 1),
    round(sum(cp_pool$xB)/tot.demand[2]*100, 1)
  )
)
```

::: columns
::: {.column width="50%"}

```{r cp-kpi-table}
#| echo: false
#| warning: false
#| message: false

kbl(kpi_df,
    col.names = c("Kennzahl", "Dezentral", "Pooling einseitig", "Pooling zweiseitig"),
    align = c("l", "r", "r", "r")) |>
  kable_styling(bootstrap_options = c("condensed", "striped"),
                full_width = TRUE, font_size = 20) |>
  column_spec(1) |>
  column_spec(2, background = "#F3E7AE") |>
  column_spec(3, bold = TRUE, background = "#E8F4F1") |>
  row_spec(0, background = "#2F3E46", color = "white", bold = TRUE)
```


:::
::: {.column width="48%"}

::: {.fragment .fade-up}

```{r cp-kpi-bar}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 4

metriken <- c("Auslastung\nWerk 1 (%)","Auslastung\nWerk 2 (%)", "Verkaufsquote\nMarkt A (%)", "Verkaufsquote\nMarkt B (%)")
dez_vals  <- kpi_df$Dezentral[c(7:10)]
pool1_vals <- kpi_df$Pooling.einseitig[c(7:10)]
pool2_vals <- kpi_df$Pooling.zweiseitig[c(7:10)]

fig_kpi <- plot_ly()

fig_kpi <- fig_kpi %>% add_trace(
  x = metriken, y = dez_vals,
  type = "bar", name = "Dezentral",
  marker = list(color = "#E9C46A",
                line = list(color = "#B8951A", width = 1)),
  text = paste0(dez_vals, " %"),
  textposition = "outside",
  textfont = list(size = 13, family = "Arial", color = "#1F2933")
)

fig_kpi <- fig_kpi %>% add_trace(
  x = metriken, y = pool1_vals,
  type = "bar", name = "Pooling einseitig",
  marker = list(color = cols$top,
                line = list(color = "#01696f", width = 1)),
  text = paste0(pool1_vals, " %"),
  textposition = "outside",
  textfont = list(size = 13, family = "Arial", color = "#1F2933")
)

fig_kpi <- fig_kpi %>% add_trace(
  x = metriken, y = pool2_vals,
  type = "bar", name = "Pooling zweiseitig",
  marker = list(color = cols$mid,
                line = list(color = "#a16a61", width = 1)),
  text = paste0(pool2_vals, " %"),
  textposition = "outside",
  textfont = list(size = 13, family = "Arial", color = "#1F2933")
)

fig_kpi <- fig_kpi %>% layout(
  barmode = "group",
  xaxis = list(title = "", fixedrange = TRUE),
  yaxis = list(title = "Wert (%)", fixedrange = TRUE,
               range = c(90, 102)),
  legend = list(orientation = "h", y = -0.18),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 50, r = 20, t = 30, b = 60),
  height = 370
)

fig_kpi
```

:::

:::
:::

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## Nutzen des Kapazitätspooling
Kapazitätspooling verbessert **alle** Servicekennzahlen gleichzeitig: mehr Verkäufe, höhere Auslastung, weniger Leerkapazität – ohne zusätzliche Produktionskapazität.
:::
:::

::: {.fragment .fade-up}
::: {.callout-warning appearance="simple"}
## Kosten des Kapazitätspooling
Kapazitätspooling benötigt jedoch zusätzliche logistische Kapazitäten und vor allem technologische Flexibilität
:::
:::

---

## Kapazitätspooling {#slide-chaining}
::: slide-subtitle
Chaining
:::

::: {.callout-note appearance="minimal"}
**Chaining**

Kapazitätspooling ermöglicht die gemeinsame Nutzung von Kapazitäten für verschiedene Märkte. Es erfordert jedoch technologische und logistische Flexibilität. Je mehr Märkte und Produkte zu bedienen sind, desto komplexer und kostspieliger ist Kapazitätspooling. Die Frage ist also, wie Kapazitätspooling bei mehreren Produkten und Märkten organisiert werden kann?
:::

::::: {.smaller-text}
:::: columns

::: {.column width="33%"}
::: {.fragment .fade-up}
**vollständiges Chaining**

```{r cp-full-chain1}
#| echo: false
#| warning: false
#| message: false
#| out-width: "95%"
#| fig-height: 3.2

draw_network <- function(edges, plants, markets, r = 0.055, range.x = c(0,1), range.y = c(0,1), width = 120, height = 320 ) {
  fig <- plot_ly(type = "scatter", mode = "markers")

  for (i in seq_len(nrow(edges))) {
    p  <- plants[edges$from[i], ]
    m  <- markets[edges$to[i], ]
    dx <- m$x - p$x; dy <- m$y - p$y; d <- sqrt(dx^2+dy^2)
    ax <- p$x + r*dx/d; ay <- p$y + r*dy/d
    ex <- m$x - r*dx/d; ey <- m$y - r*dy/d

    if (edges$dash[i]) {
      n_seg <- 12
      xs <- seq(ax, ex, length.out = n_seg+1)
      ys <- seq(ay, ey, length.out = n_seg+1)
      for (s in seq(1, n_seg-1, by=2)) {
        fig <- fig %>% add_segments(
          x = xs[s], xend = xs[s+1], y = ys[s], yend = ys[s+1],
          line = list(color="#4B4B4B", width=1.6),
          hoverinfo="skip", showlegend=FALSE
        )
      }
      fig <- fig %>% add_annotations(
        x=ex, y=ey, ax=xs[n_seg], ay=ys[n_seg],
        xref="x", yref="y", axref="x", ayref="y",
        showarrow=TRUE, arrowhead=2, arrowsize=0.9,
        arrowwidth=1.6, arrowcolor="#4B4B4B", text=""
      )
    } else {
      fig <- fig %>% add_annotations(
        x=ex, y=ey, ax=ax, ay=ay,
        xref="x", yref="y", axref="x", ayref="y",
        showarrow=TRUE, arrowhead=2, arrowsize=0.9,
        arrowwidth=2.0, arrowcolor="#222222", text=""
      )
    }
  }

  # Werke-Knoten (Hexagon)
  fig <- fig %>% add_trace(
    x=plants$x, y=plants$y, text=plants$lbl, mode="markers+text",
    marker=list(symbol="hexagon", size=36,
                color="#BFDBF7", line=list(color="#4B5563",width=1.5)),
    textfont=list(size=14, color="#1F2933", family="Arial"),
    textposition="middle center", hoverinfo="skip", showlegend=FALSE
  )

  # Markt-Knoten (Circle)
  fig <- fig %>% add_trace(
    x=markets$x, y=markets$y, text=markets$lbl, mode="markers+text",
    marker=list(symbol="circle", size=36,
                color="#2D9E8E", line=list(color="#2F3E46",width=1.5)),
    textfont=list(size=14, color="white", family="Arial"),
    textposition="middle center", hoverinfo="skip", showlegend=FALSE
  )

  fig %>% layout(
    xaxis=list(visible=FALSE, range=range.x, fixedrange=TRUE),
    yaxis=list(visible=FALSE, range=range.y, fixedrange=TRUE),
    plot_bgcolor="transparent", paper_bgcolor="transparent",
    margin=list(l=10,r=10,t=30,b=10), height=height, width=width
  )
}


plants  <- data.frame(id = paste0("P",1:4), lbl = as.character(1:4),
                      x = 0.20, y = c(0.85,0.58,0.38,0.12))
markets <- data.frame(id = c("A","B","C","D"), lbl = c("A","B","C","D"),
                      x = 0.78, y = c(0.85,0.58,0.38,0.12))
rownames(plants)  <- plants$id
rownames(markets) <- markets$id

r <- 0.055

edges <- data.frame(
  from = c("P1","P2","P3","P4",   "P1","P2","P3","P4",   "P1","P2","P3","P4",   "P1","P2","P3","P4"),
  to   = c("A","B","C","D",       "B","C","D","A",       "C","D","A","B",       "D","A","B","C"),
  dash = c(rep(FALSE,4),          rep(TRUE,4),          rep(TRUE,4),          rep(TRUE,4)),
  stringsAsFactors = FALSE
)

draw_network(edges, plants, markets, range.y = c(.05,.925), width = 300, height = 300)

```

Verbindungen: $n \cdot n = 16$

Technologien je Werk: $n=4$

Märkte je Werk: $n=4$
:::
:::

::: {.column width="33%"}

::: {.fragment .fade-up}
**Kurzes Chaining**  

```{r chaining-short}
#| echo: false
#| warning: false
#| message: false
#| out-width: "95%"
#| fig-height: 3.2

# Solid: 1->A, 2->B, 3->C, 4->D
# Dashed (short chain, next neighbour +1): 1->B, 2->C, 3->D, 4->A  (wrap-around)
# Dashed cross-down: 2->A, 3->B  etc. (Cachon: "short" = neighbor skip)
edges <- data.frame(
  from = c("P1","P2","P3","P4",   "P1","P2","P3","P4"),
  to   = c("A","B","C","D",       "B","A","D","C"),
  dash = c(rep(FALSE,4),          rep(TRUE,4)),
  stringsAsFactors = FALSE
)

draw_network(edges, plants, markets, range.y = c(.05,.925), width = 300, height = 300)
```

Verbindungen: $2 \cdot n = 8$

Technologien je Werk: $n=2$

Märkte je Werk: $n=2$
:::
:::


::: {.column width="33%"}

::: {.fragment .fade-up}
**Langes Chaining**  

```{r chaining-long}
#| echo: false
#| warning: false
#| message: false
#| out-width: "95%"
#| fig-height: 3.2

# Long SC alternative: Solid: 1->A, 2->B, 3->C, 4->D
# Dashed (long chain, skip +2): 1->C, 2->D, 3->A, 4->B
edges_long <- data.frame(
  from = c("P1","P2","P3","P4",   "P1","P2","P3","P4"),
  to   = c("A","B","C","D",       "B","C","D","A"),
  dash = c(rep(FALSE,4),          rep(TRUE,4)),
  stringsAsFactors = FALSE
)

draw_network(edges_long, plants, markets, range.y = c(.05,.925), width = 300, height = 300)
```

Verbindungen: $2 \cdot n = 8$

Technologien je Werk: $n=2$

Märkte je Werk: $n=4$

:::
:::
::::
:::::

---

## Umgang mit Unsicherheit {#slide-inv-pooling-intro}
::: slide-subtitle
Pooling über die Zeit - Sicherheitsbestände 
:::

::: {.callout-note appearance="minimal" icon=false}
## Definition Sicherheitsbestand (Wiederholung)

Sicherheitsbestände gleichen Nachfrageschwankungen über die Zeit aus. Sei $S$ der Anfangslagerbestand zu Begin der Risikoperiode und $\mu$ der erwartete Bedarf in der Risikoperiode, dann gilt für den Sicherheitsbestand $sb$: $sb = S - \mu$
:::

:::: columns
::: {.column width="33%"}

::: {.fragment .fade-up}
::: {.callout-note appearance="minimal" icon=false}
## Festlegung von Sicherheitsbeständen

- *nach Kosten*: Sicherheitsbestände verursachen Lagerhaltungskosten und vermeiden Fehlmengenkosten $\rightarrow$ Kostenabwägung 
- *Lieferbereitschaft*: Absicherung des Risikos, im Risikozeitraum nicht lieferfähig zu sein ($\alpha$-Servicegrad)
- *Versorgungsgrad*: Anteil der Nachfrage, die langfristig innerhalb des Risikozeitraums erfüllt werden soll ($\beta$-Servicegrad)
:::
:::
:::


::: {.column width="65%"}

::: {.fragment .fade-up}

```{r ts-lager-graph}
#| echo: false
#| warning: false
#| message: false
#| out-width: "95%"
#| fig-height: 6

set.seed(42)
n   <- 24        # Perioden
lam <- 20        # mittlere Nachfrage (Poisson)
t   <- 4         # Bestellintervall
S   <- 135       # Order-up-to-Level
wbz <- 2         # Wiederbeschaffungszeit (Perioden)
l.ini <- 120     # Anfangslagerbestand
sb <- S - (t + wbz) * lam

demand <- rpois(n, lambda = lam)

# --- Simulation ---
res <- tS.lager(d = demand, t = t, S = S, l.ini = l.ini, wbz = wbz)
res <- as.data.frame(res)

# --- Subset: nur Perioden 2 bis 22 ---
idx      <- 2:22
perioden <- seq_len(nrow(res))[idx]
res_sub  <- res[idx, ]

# --- Doppelpfeile: Länge 3.95, lückenlos nebeneinander ---
arrow_len <- t - 0.05          # 3.95 (leicht kürzer als Bestellintervall t=4)
x_start   <- perioden[1]
x_end     <- perioden[length(perioden)]

# Pfeil-Startpunkte: lückenlos ab x_start
arrow_x0 <- seq(x_start, x_end - arrow_len + 0.01, by = arrow_len)
arrow_x1 <- arrow_x0 + arrow_len

# y-Position der Pfeile: knapp unterhalb Nulllinie
y_min_data <- min(pmin(0, res_sub$Lagerbestand), na.rm = TRUE)
y_arrow    <- y_min_data - abs(y_min_data) * 0.15 - 5

fig <- plot_ly()

# Lagerbestand
fig <- fig |> add_lines(
  x = perioden, y = res_sub$Lagerbestand,
  line = list(shape = "hvh"), name = "Lagerbestand"
)

# Sicherheitsbestand
fig <- fig |> add_lines(
  x = perioden, y = sb,
  name = "Sicherheitsbestand"
)

# Fehlmengen-Füllung
fig <- fig |> add_trace(
  x = perioden, y = pmin(0, res_sub$Lagerbestand),
  type = "scatter", mode = "lines",
  line = list(shape = "hvh", color = "transparent"),
  name = "Fehlmengen",
  fill = "tozeroy", fillcolor = "#FF000050"
)

# --- Doppelpfeile zeichnen ---
for (i in seq_along(arrow_x0)) {
  x0 <- arrow_x0[i]
  x1 <- arrow_x1[i]
  xm <- (x0 + x1) / 2

  # Horizontale Linie (Schaft des Doppelpfeils)
  fig <- fig |> add_segments(
    x = x0, xend = x1,
    y = y_arrow, yend = y_arrow,
    line = list(color = "#333333", width = 1.5),
    showlegend = FALSE, hoverinfo = "skip"
  )

  # Linke Pfeilspitze (zeigt nach links)
  fig <- fig |> add_annotations(
    x = x0, y = y_arrow,
    ax = x0 + 0.6, ay = y_arrow,
    xref = "x", yref = "y", axref = "x", ayref = "y",
    showarrow = TRUE, arrowhead = 2, arrowsize = 0.9,
    arrowwidth = 1.5, arrowcolor = "#333333", text = ""
  )

  # Rechte Pfeilspitze (zeigt nach rechts)
  fig <- fig |> add_annotations(
    x = x1, y = y_arrow,
    ax = x1 - 0.6, ay = y_arrow,
    xref = "x", yref = "y", axref = "x", ayref = "y",
    showarrow = TRUE, arrowhead = 2, arrowsize = 0.9,
    arrowwidth = 1.5, arrowcolor = "#333333", text = ""
  )

  # Beschriftung "Periode" zentriert unter dem Pfeil
  fig <- fig |> add_annotations(
    x = xm, y = y_arrow,
    xref = "x", yref = "y",
    text = "Periode", showarrow = FALSE,
    yshift = -16,
    font = list(size = 11, color = "#333333", family = "Arial")
  )
}

fig <- fig |> layout(
  legend = list(
    orientation = "h",
    x           = 0.5,
    y           = 1.12,
    xanchor     = "center",
    yanchor     = "top"
  ),
  xaxis = list(
    showticklabels = FALSE,     # keine Zahlen auf der x-Achse
    showgrid       = FALSE,
    zeroline       = FALSE,
    range          = c(x_start - 0.3, x_end + 0.3)
  ),
  yaxis = list(
    title = "Bestand",
    range = c(y_arrow - 10, NA)  # y-Achse nach unten erweitern für Pfeile
  ),
  margin = list(b = 55),           # Randplatz für Beschriftung unterhalb
  height= 550,
  width = 1000
)

fig
```

:::
:::



::::

---

## Lagerbestandspooling {#slide-inv-pooling-intro}
::: slide-subtitle
Allokation von Sicherheitsbeständen 
:::

::: {.callout-note appearance="minimal" icon=false}
**Lagerbestandspooling** (*inventory pooling*) bezeichnet die Bündelung von Sicherheitsbeständen mehrerer dezentraler Lager in einem **zentralen Lager**. Durch den **Risikoausgleichseffekt** sinkt der benötigte Gesamtsicherheitsbestand bei gleichem Servicegrad.
:::

::: columns
::: {.column width="52%"}

::: {.fragment .fade-up}
::: smaller-text

**Dezentral vs. Zentral**

```{r inv-pooling-diagram}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 3.6

# Schematische Darstellung: n dezentrale Lager vs. 1 zentrales Lager
n_demo <- 4

# Dezentrale Lager
x_dez <- seq(0.15, 0.85, length.out = n_demo)
y_lager <- 0.65
y_kunde <- 0.15
y_zentrallager <- 0.65
y_verteiler <- 0.15

fig_dia <- plot_ly()

# ─── DEZENTRAL (links) ───
for (i in seq_along(x_dez)) {
  xi <- x_dez[i] * 0.48
  # Lager-Symbol (Rechteck)
  fig_dia <- fig_dia %>% add_annotations(
    x = xi, y = y_lager, xref = "paper", yref = "paper",
    text = paste0("🏭<br><b>L", i, "</b>"),
    showarrow = FALSE,
    font = list(size = 11, color = "#0B6E4F", family = "Arial"),
    align = "center"
  )
  # Kunde
  fig_dia <- fig_dia %>% add_annotations(
    x = xi, y = y_kunde, xref = "paper", yref = "paper",
    text = paste0("👤<br>K", i),
    showarrow = FALSE,
    font = list(size = 11, color = "#1D4ED8", family = "Arial"),
    align = "center"
  )
}

fig_dia <- fig_dia %>% add_annotations(
  x = 0.12, y = 0.95, xref = "paper", yref = "paper",
  text = "<b>Dezentral: n = 4 Lager</b>",
  showarrow = FALSE,
  font = list(size = 13, color = "#1F2933", family = "Arial"),
  align = "left"
)

# ─── ZENTRAL (rechts) ───
x_zentral <- 0.74
x_kunden_z <- seq(0.56, 0.92, length.out = n_demo)

fig_dia <- fig_dia %>% add_annotations(
  x = x_zentral, y = y_zentrallager, xref = "paper", yref = "paper",
  text = "🏢<br><b>Zentral-<br>lager</b>",
  showarrow = FALSE,
  font = list(size = 12, color = "#0B6E4F", family = "Arial"),
  align = "center"
)

for (i in seq_along(x_kunden_z)) {
  fig_dia <- fig_dia %>% add_annotations(
    x = x_kunden_z[i], y = y_verteiler, xref = "paper", yref = "paper",
    text = paste0("👤<br>K", i),
    showarrow = FALSE,
    font = list(size = 11, color = "#1D4ED8", family = "Arial"),
    align = "center"
  )
}

fig_dia <- fig_dia %>% add_annotations(
  x = 0.60, y = 0.95, xref = "paper", yref = "paper",
  text = "<b>Zentral: 1 Lager</b>",
  showarrow = FALSE,
  font = list(size = 13, color = "#1F2933", family = "Arial"),
  align = "left"
)

# Trennlinie
fig_dia <- fig_dia %>% add_annotations(
  x = 0.51, y = 0.5, xref = "paper", yref = "paper",
  text = "vs.",
  showarrow = FALSE,
  font = list(size = 18, color = "#6B7280", family = "Arial"),
  align = "center"
)

fig_dia <- fig_dia %>% layout(
  xaxis = list(visible = FALSE, fixedrange = TRUE),
  yaxis = list(visible = FALSE, fixedrange = TRUE),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 10, r = 10, t = 10, b = 10),
  height = 270
)

fig_dia
```

:::
:::

:::
::: {.column width="46%"}

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## Risikoausgleich

- Schwankungen einzelner Standorte **kompensieren sich** gegenseitig
- Die **Standardabweichung der Gesamtnachfrage** wächst langsamer als die Anzahl der Standorte:

$$\sigma_\text{pool} = \sqrt{n} \cdot \sigma \quad (\text{bei Unabhängigkeit})$$

- Sicherheitsbestand dezentral: $n \cdot z \cdot \sigma$
- Sicherheitsbestand zentral: $z \cdot \sqrt{n} \cdot \sigma$
- **Einsparung:** $z \cdot \sigma \cdot (n - \sqrt{n})$
:::
:::

:::
:::

---

## Lagerbestandspooling – Formales Modell {#slide-inv-pooling-model}
::: slide-subtitle
Sicherheitsbestand: Dezentral vs. zentral (Cachon & Terwiesch, Kap. 13)
:::

::: columns
::: {.column width="50%"}

::: {.callout-note appearance="minimal" icon=false}
**Notation**

- $n$: Anzahl dezentraler Standorte
- $\sigma$: Standardabweichung der Nachfrage je Standort (pro Wiederbeschaffungsperiode)
- $z$: Servicefaktor ($z = \Phi^{-1}(\beta)$ für Servicegrad $\beta$)
:::

::: {.fragment .fade-up}
::: smaller-text

**Sicherheitsbestände im Vergleich**

| Konfiguration | Formel | Beispiel ($n=4$, $\sigma=50$, $z=1{,}65$) |
|---|---|---|
| **Dezentral** (je Lager) | $SS_i = z \cdot \sigma$ | $= 82{,}5$ |
| **Dezentral** (gesamt) | $SS_\text{dez} = n \cdot z \cdot \sigma$ | $= 330$ |
| **Zentral** | $SS_\text{pool} = z \cdot \sqrt{n} \cdot \sigma$ | $= 165$ |
| **Einsparung** | $\Delta SS = z \cdot \sigma \cdot (n - \sqrt{n})$ | $= 165$ (50%) |

:::
:::

:::
::: {.column width="48%"}

::: {.fragment .fade-up}

```{r inv-pooling-model-plot}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 4.8

sigma_ex <- 50
z_ex     <- qnorm(0.95)
n_seq    <- 1:20

ss_dez   <- n_seq * z_ex * sigma_ex
ss_pool  <- z_ex * sqrt(n_seq) * sigma_ex
savings  <- ss_dez - ss_pool

df_model <- data.frame(n = n_seq, ss_dez, ss_pool, savings)

fig_model <- plot_ly(df_model, x = ~n)

fig_model <- fig_model %>% add_trace(
  y = ~ss_dez, type = "scatter", mode = "lines+markers",
  name = "SS dezentral (gesamt)",
  line   = list(color = "#E76F51", width = 2),
  marker = list(color = "#E76F51", size = 5)
)
fig_model <- fig_model %>% add_trace(
  y = ~ss_pool, type = "scatter", mode = "lines+markers",
  name = "SS zentral (Pool)",
  line   = list(color = cols$top, width = 2.5),
  marker = list(color = cols$top, size = 5)
)
fig_model <- fig_model %>% add_trace(
  y = ~savings, type = "scatter", mode = "lines",
  name = "Einsparung ΔSS",
  line = list(color = "#a12c7b", width = 2, dash = "dot"),
  yaxis = "y2"
)

fig_model <- fig_model %>% layout(
  xaxis  = list(title = "Anzahl Standorte n", dtick = 2, fixedrange = TRUE),
  yaxis  = list(title = "Sicherheitsbestand (Einh.)", fixedrange = TRUE),
  yaxis2 = list(title = "Einsparung ΔSS (Einh.)",
                overlaying = "y", side = "right",
                fixedrange = TRUE, showgrid = FALSE),
  legend = list(orientation = "h", y = -0.22, font = list(size = 11)),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 55, r = 55, t = 15, b = 70),
  height = 380
)

fig_model
```

:::

:::
:::

---

## Lagerbestandspooling – Berechnung {#slide-inv-pooling-calc}
::: slide-subtitle
Anwendung von `pooling_safety_stock()` aus `scm_functions.R`
:::

::: columns
::: {.column width="52%"}

```{r inv-pooling-calc}
#| echo: true
#| warning: false
#| message: false

# Servicegrad 95 % (z = 1,645)
res <- pooling_safety_stock(
  n     = 8,
  sigma = 40,
  z     = qnorm(0.95)
)
```

::: small-text

```{r inv-pooling-result-table}
#| echo: false
#| warning: false
#| message: false

result_df <- data.frame(
  Kennzahl = c(
    "Servicegrad-Faktor z",
    "SS je dezentralem Lager",
    "SS dezentral (gesamt, n = 8)",
    "σ zentrales Lager (√n · σ)",
    "SS zentrales Lager",
    "Absolute Einsparung ΔSS",
    "Relative Einsparung (%)"
  ),
  Formel = c(
    "$z = \\Phi^{-1}(0{,}95)$",
    "$z \\cdot \\sigma$",
    "$n \\cdot z \\cdot \\sigma$",
    "$\\sqrt{n} \\cdot \\sigma$",
    "$z \\cdot \\sqrt{n} \\cdot \\sigma$",
    "$n \\cdot z \\cdot \\sigma - z \\cdot \\sqrt{n} \\cdot \\sigma$",
    "$1 - 1/\\sqrt{n}$"
  ),
  Wert = c(
    round(res$z, 3),
    round(res$ss_individual, 1),
    round(res$ss_total_individual, 1),
    round(res$sigma_central, 1),
    round(res$ss_central, 1),
    round(res$savings, 1),
    paste0(round(res$savings_pct, 1), " %")
  )
)

kbl(result_df,
    col.names = c("Kennzahl", "Formel", "Wert"),
    align = c("l", "l", "r"),
    escape = FALSE) |>
  kable_styling(bootstrap_options = c("condensed", "striped"),
                full_width = TRUE, font_size = 17) |>
  column_spec(1, bold = TRUE) |>
  column_spec(3, bold = TRUE, background = "#E8F4F1") |>
  row_spec(0, background = "#2F3E46", color = "white", bold = TRUE) |>
  row_spec(c(5, 6, 7), background = "#F0FDF4")
```

:::

:::
::: {.column width="46%"}

::: {.fragment .fade-up}
::: {.callout-tip appearance="simple"}
## Ergebnis ($n = 8$, $\sigma = 40$, $\beta = 95\%$)

Durch Zentralisierung sinkt der Gesamtsicherheitsbestand von
**`r round(res$ss_total_individual, 0)` auf `r round(res$ss_central, 0)` Einheiten** —
eine Einsparung von **`r round(res$savings_pct, 1)` %**.

Der **Pooling-Faktor** beträgt $\sqrt{`r res$n`} = `r round(res$pooling_factor, 3)`$.
:::
:::

::: {.fragment .fade-up}
::: {.callout-warning appearance="simple"}
## Annahmen
- Nachfragen der $n$ Standorte sind **unabhängig** und **identisch verteilt**
- Gemeinsamer Servicegrad $\beta$ für das Zentrallager
- **Keine Transportzeitunterschiede** zwischen zentralem und dezentralem System
:::
:::

:::
:::

---

## Lagerbestandspooling – Sensitivität {#slide-inv-pooling-sensitivity}
::: slide-subtitle
Einsparung in Abhängigkeit von $n$ und $\sigma$
:::

::: columns
::: {.column width="52%"}

```{r inv-pooling-sensitivity}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 5.0

z_sens <- qnorm(0.95)
n_grid <- c(2, 4, 6, 8, 10, 12, 16, 20)
sigma_grid <- c(20, 40, 60, 80)

# Relative Einsparung (hängt nur von n ab!)
rel_savings <- sapply(n_grid, function(n) (1 - 1/sqrt(n)) * 100)

df_rel <- data.frame(n = n_grid, rel = rel_savings)

fig_sens <- plot_ly(df_rel, x = ~n, y = ~rel,
  type = "scatter", mode = "lines+markers",
  name = "Relative Einsparung (%)",
  line   = list(color = cols$top, width = 2.5),
  marker = list(color = cols$top, size = 8),
  text   = paste0(round(rel_savings, 1), " %"),
  textposition = "top center",
  textfont = list(size = 11, family = "Arial", color = "#1F2933")
)

fig_sens <- fig_sens %>% layout(
  xaxis = list(title = "Anzahl Standorte n", dtick = 2,
               tickvals = n_grid, fixedrange = TRUE),
  yaxis = list(title = "Relative SS-Einsparung (%)",
               range = c(0, 80), fixedrange = TRUE),
  annotations = list(list(
    x = 18, y = 73,
    text = "<b>Unabhängig von σ!</b>",
    showarrow = FALSE,
    font = list(size = 11, color = "#a12c7b", family = "Arial")
  )),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 55, r = 20, t = 20, b = 60),
  height = 370
)

fig_sens
```

:::
::: {.column width="46%"}

::: {.fragment .fade-up}

```{r inv-pooling-abs-savings}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 4.6

# Absolute Einsparung je σ-Wert
fig_abs <- plot_ly()

farben_abs <- c("#52B788", "#0B6E4F", "#E9C46A", "#E76F51")

for (s_idx in seq_along(sigma_grid)) {
  s <- sigma_grid[s_idx]
  abs_sav <- sapply(n_grid, function(n) {
    r <- pooling_safety_stock(n = n, sigma = s, z = z_sens)
    r$savings
  })
  fig_abs <- fig_abs %>% add_trace(
    x = n_grid, y = abs_sav,
    type = "scatter", mode = "lines+markers",
    name = paste0("σ = ", s),
    line   = list(color = farben_abs[s_idx], width = 2),
    marker = list(color = farben_abs[s_idx], size = 6)
  )
}

fig_abs <- fig_abs %>% layout(
  xaxis = list(title = "Anzahl Standorte n", dtick = 2,
               tickvals = n_grid, fixedrange = TRUE),
  yaxis = list(title = "Absolute SS-Einsparung (Einh.)",
               fixedrange = TRUE),
  legend = list(orientation = "h", y = -0.20, font = list(size = 11)),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 55, r = 20, t = 15, b = 70),
  height = 340
)

fig_abs
```

:::

::: {.fragment .fade-up}
::: {.callout-note appearance="minimal" icon=false}
**Kernerkenntnis:** Die **relative** Einsparung $(1 - 1/\sqrt{n})$ hängt ausschließlich von $n$ ab — nicht von $\sigma$. Die **absolute** Einsparung hingegen steigt proportional mit $\sigma$.
:::
:::

:::
:::

---

## Lagerbestandspooling – Grenzen {#slide-inv-pooling-limits}
::: slide-subtitle
Wann lohnt sich Zentralisierung nicht?
:::

::: columns
::: {.column width="50%"}

::: {.callout-warning appearance="simple"}
## Grenzen des Inventory Poolings

1. **Positive Korrelation** ($\rho > 0$): Nachfragen schwanken gleichgerichtet – der Risikoausgleich nimmt ab. Allgemein gilt:
$$SS_\text{pool} = z \cdot \sigma \cdot \sqrt{n + n(n-1)\rho}$$

2. **Längere Lieferzeiten:** Ein zentrales Lager kann weiter von Kunden entfernt sein $\rightarrow$ höhere Wiederbeschaffungszeit erhöht $\sigma$

3. **Höhere Transportkosten:** Direktlieferung vom Zentrallager zu Kunden kann teurer sein als lokale Lager

4. **Servicelevel-Anforderungen:** Sehr kurze Lieferzeitfenster erfordern physische Nähe
:::

:::
::: {.column width="48%"}

::: {.fragment .fade-up}

```{r inv-pooling-rho-effect}
#| echo: false
#| warning: false
#| message: false
#| out-width: "99%"
#| fig-height: 5.2

z_lim  <- qnorm(0.95)
n_lim  <- 8
sig_lim <- 40
rho_lim <- seq(0, 1, by = 0.05)

# SS Pool mit Korrelation: z * sigma * sqrt(n + n*(n-1)*rho)
ss_pool_rho <- z_lim * sig_lim * sqrt(n_lim + n_lim * (n_lim - 1) * rho_lim)
ss_dez_lim  <- n_lim * z_lim * sig_lim   # konstant

df_lim <- data.frame(rho = rho_lim, ss_pool = ss_pool_rho, ss_dez = ss_dez_lim)

fig_lim <- plot_ly(df_lim, x = ~rho)

fig_lim <- fig_lim %>% add_trace(
  y = ~ss_dez,
  type = "scatter", mode = "lines",
  name = "SS dezentral (gesamt)",
  line = list(color = "#6B7280", width = 1.8, dash = "dash"),
  hoverinfo = "skip"
)
fig_lim <- fig_lim %>% add_trace(
  y = ~ss_pool,
  type = "scatter", mode = "lines",
  name = "SS Pool (mit Korrelation ρ)",
  line = list(color = cols$top, width = 2.5),
  fill = "tonexty",
  fillcolor = "rgba(11,110,79,0.12)"
)

fig_lim <- fig_lim %>% layout(
  xaxis = list(title = "Korrelation ρ zwischen Standorten",
               fixedrange = TRUE, dtick = 0.2),
  yaxis = list(title = "Sicherheitsbestand (Einh.)", fixedrange = TRUE),
  annotations = list(
    list(x = 0.05, y = ss_pool_rho[2] * 0.85,
         text = "<b>Pool-Vorteil</b>",
         showarrow = FALSE,
         font = list(size = 12, color = cols$top, family = "Arial")),
    list(x = 0.90, y = ss_dez_lim * 1.02,
         text = "<b>kein Vorteil</b>\nbei ρ → 1",
         showarrow = FALSE,
         font = list(size = 12, color = "#6B7280", family = "Arial"))
  ),
  legend = list(orientation = "h", y = -0.20, font = list(size = 11)),
  plot_bgcolor  = "transparent",
  paper_bgcolor = "transparent",
  margin = list(l = 55, r = 20, t = 15, b = 70),
  height = 400
)

fig_lim
```

:::

:::
:::
